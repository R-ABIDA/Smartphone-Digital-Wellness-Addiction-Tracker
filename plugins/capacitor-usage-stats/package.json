{
  "name": "capacitor-usage-stats",
  "version": "1.0.0",
  "description": "Local Capacitor plugin exposing real Android UsageStatsManager data (screen time, pickups, per-app usage) to the SmartTracker web app. iOS has no equivalent public API and falls back to the web stub.",
  "main": "dist/plugin.cjs.js",
  "module": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "files": [
    "android/src/main/",
    "android/build.gradle",
    "dist/"
  ],
  "scripts": {
    "build": "npm run clean && tsc && rollup -c rollup.config.js",
    "clean": "rimraf ./dist"
  },
  "capacitor": {
    "android": {
      "src": "android"
    }
  },
  "peerDependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "rimraf": "^5.0.0",
    "rollup": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
